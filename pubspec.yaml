name: portfolio
description: Personal portfolio

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+1

# Build command: flutter build web --web-renderer canvaskit --release

environment:
  sdk: ">=2.14.0"

dependencies:
  flutter:
    sdk: flutter
  fimber: 0.6.1  # Logging
  get_it: 7.2.0  # Service locator
  easy_localization: 3.0.1  # Localization
  functional_widget_annotation: 0.9.0+1  # Make things more readable without losing performance
  intl: ^0.17.0
  flutter_svg: 0.22.0
  url_launcher: 6.0.12
  animated_text_kit: 4.2.1
  carousel_slider: 4.0.0  # Used to display screenshots for the Zappr project
  video_player: 2.2.5  # Play videos
  http: ^0.13.4  # Send the email
  progress_state_button: ^1.0.3  # Send button to send the email
  url_strategy: ^0.2.0  # Remove the '#' from the url on the web
  visibility_detector: ^0.3.3   # Provide a callback when a fraction of the viewport is visible



  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_driver:
    sdk: flutter
  build_runner: 2.1.4
  functional_widget: 0.9.1
  mockito: ^5.0.16

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/images/
    - assets/images/zappr/
    - assets/images/chat_ui_kit/
    - assets/translations/
    - assets/logos/
    - assets/fonts/
    - assets/videos/

  fonts:
    - family: IBMPlexMono
      fonts:
        - asset: assets/fonts/IBMPlexMono-Regular.ttf
        - asset: assets/fonts/IBMPlexMono-Bold.ttf
    - family: CustomIcons
      fonts:
        - asset: assets/fonts/CustomIcons.ttf